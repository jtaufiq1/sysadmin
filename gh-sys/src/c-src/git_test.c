#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// this is to just test that I pull and push correctly on git and github with ease and satisfaction

int main()
      {
        char *name[100];
	char age[20];
	int i;

        printf("What is your name:");
        scanf("%[^\n]s",&name);

        // print your name now 
	i = strlen(name[i]);
        printf("Hi! %i.\nWelcome to Git and Github\n", name[i]);

  return 0;
      }
      
